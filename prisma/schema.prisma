// ... existing code ...

model Product {
  // ... existing fields ...
  supplierUrl    String?
  supplierPrice  Float?
  lastSourced    DateTime?

  // REPLACE THE OLD INDEXES WITH THIS:
  // This helps finding "supplierUrl: null" AND sorting by "lastSourced" at the same time
  @@index([supplierUrl, lastSourced(sort: Desc)])
  @@index([createdAt(sort: Desc)])
}